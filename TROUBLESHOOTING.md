# Troubleshooting

If the desktop window cannot connect, remove no data: inspect the server log and verify loopback port availability. If the Windows Agent is offline, tool actions remain unavailable and the health panel reports degradation. If persistence is degraded, create a backup after recovery and check the `runtime` and `migrations` directories. Updates remain disabled for unsigned or non-HTTPS configurations.
