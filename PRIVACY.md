# LOHZ Privacy

LOHZ stores user-scoped memories, execution state, health metrics, and explicit backups in the OS application-data directory. Unknown participants remain session-scoped. Raw audio and voiceprints are not stored by default. Credentials are encrypted, and the encryption key is OS-protected when Electron safe storage is available. Users can create backups and explicitly restore them; upgrades do not delete user data.
